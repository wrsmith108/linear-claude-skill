{
  "name": "claude-plugin-linear",
  "version": "2.6.2",
  "type": "module",
  "description": "Claude Code skill for Linear issue, project, and team management with MCP integration",
  "keywords": [
    "claude",
    "claude-code",
    "claude-plugin",
    "linear",
    "project-management",
    "issue-tracking",
    "workflow"
  ],
  "author": "wrsmith108",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/wrsmith108/linear-claude-skill.git"
  },
  "homepage": "https://github.com/wrsmith108/linear-claude-skill#readme",
  "bugs": {
    "url": "https://github.com/wrsmith108/linear-claude-skill/issues"
  },
  "files": [
    "SKILL.md",
    "scripts",
    "dist",
    "docs",
    "hooks",
    "api.md",
    "sdk.md",
    "sync.md",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "node scripts/build.mjs",
    "prebuild": "npm run typecheck",
    "prepublishOnly": "npm run build",
    "setup": "f() { [ -f dist/setup.js ] && node dist/setup.js \"$@\" || (echo '[WARN] dist/ not built, using tsx (slower). Run: npm run build' >&2 && npx tsx scripts/setup.ts \"$@\"); }; f",
    "ops": "f() { [ -f dist/linear-ops.js ] && node dist/linear-ops.js \"$@\" || (echo '[WARN] dist/ not built, using tsx (slower). Run: npm run build' >&2 && npx tsx scripts/linear-ops.ts \"$@\"); }; f",
    "query": "f() { [ -f dist/query.js ] && node dist/query.js \"$@\" || (echo '[WARN] dist/ not built, using tsx (slower). Run: npm run build' >&2 && npx tsx scripts/query.ts \"$@\"); }; f",
    "sync": "f() { [ -f dist/sync.js ] && node dist/sync.js \"$@\" || (echo '[WARN] dist/ not built, using tsx (slower). Run: npm run build' >&2 && npx tsx scripts/sync.ts \"$@\"); }; f",
    "test-connection": "f() { [ -f dist/query.js ] && node dist/query.js \"query { viewer { name } }\" \"$@\" || (echo '[WARN] dist/ not built, using tsx (slower). Run: npm run build' >&2 && npx tsx scripts/query.ts \"query { viewer { name } }\" \"$@\"); }; f",
    "upload-image": "f() { [ -f dist/upload-image.js ] && node dist/upload-image.js \"$@\" || (echo '[WARN] dist/ not built, using tsx (slower). Run: npm run build' >&2 && npx tsx scripts/upload-image.ts \"$@\"); }; f",
    "extract-image": "f() { [ -f dist/extract-image.js ] && node dist/extract-image.js \"$@\" || (echo '[WARN] dist/ not built, using tsx (slower). Run: npm run build' >&2 && npx tsx scripts/extract-image.ts \"$@\"); }; f",
    "test": "node --test scripts/__tests__/smoke.test.ts",
    "typecheck": "tsc --noEmit",
    "lint": "eslint scripts/",
    "lint:fix": "eslint scripts/ --fix"
  },
  "claude-plugin": {
    "name": "Linear",
    "description": "Manage Linear issues, projects, and teams with MCP tools, SDK automation, and GraphQL API patterns",
    "skills": [
      "."
    ]
  },
  "dependencies": {
    "@linear/sdk": "^68.1.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "@semantic-release/changelog": "^6.0.3",
    "@semantic-release/git": "^10.0.1",
    "@types/node": "^25.0.10",
    "esbuild": "^0.27.4",
    "eslint": "^9.0.0",
    "semantic-release": "^25.0.2",
    "typescript": "^5.7.0",
    "typescript-eslint": "^8.0.0"
  }
}
